export type Post = {
    id: string;
    author: string;
    text: string;
    img: string;
    data_created: string;
    likes_number: string;
}

export type User = {
    id: string;
    email: string;
    password: string;
}
